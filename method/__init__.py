"""Sequential fine-tuning pipeline for the MSc thesis.

Importing this package has no side effects. Entry points load the .env file
explicitly via :func:`method.utils.load_dotenv`; worker subprocesses inherit
the parent's environment.
"""
