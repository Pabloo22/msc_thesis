
\chapter{Introduction}
\label{ch:intro}

\section{Continual Learning is a Core Aspect of Intelligence}

% Hook: anterograde amnesia
After the surgery that removed his medial temporal lobes, Henry Molaison could still recall his childhood and reason about the present, yet he had to be reintroduced to the same people day after day \citep{dossani2015legacy_molaison}. This condition, which impeded his ability to form new memories, is known as anterograde amnesia. As we can imagine, this disorder is extremely debilitating, and it is an illness that current large language models (LLMs) share.

% Continual Learning is a core aspect of intelligence
It is, therefore, not surprising that continual learning---the capacity to learn on the job in real time---is regarded as a core aspect of intelligence. François Chollet, for instance, defines intelligence as skill-acquisition efficiency \citep{chollet2019intelligence}. Another example is Richard Sutton, one of the fathers of reinforcement learning (RL), who presents continual learning as one of the core challenges that artificial intelligence (AI) researchers should tackle \citep{sutton2023albertaplanairesearch} to solve the ``grand challenge of understanding intelligence.''

% Approaches to CL - we focus on parameter-based methods
The approaches to continual learning can be roughly divided into three categories: in-context learning, storage-based methods, and parameter-based methods \citep{pacchiardi2026continual}. In-context learning works by conditioning the model on prior data, with the hope that the model will learn the patterns present in that data. The main limitation of this approach is that it is not persistent across sessions. Storage-based methods, on the other hand, store previous experience in a database that can be queried to retrieve relevant information. While this approach solves the persistence problem, it is still limited by the model's context window and its capacity to work within it. For these reasons, the most promising approach is parameter-based methods, which update the model's parameters in real time. However, it presents several challenges, such as the catastrophic forgetting of previously learned knowledge \citep{gordon1989catastrophic_interference,Luo2023catastrophic_forgetting_llms}, or ``loss of plasticity'', when the model is not able to continue learning \citep{dohare2024plasticity_loss}.

% We focus on EM
% TODO ADD: What is the relevance of solving EM for CL
% - ADD: Datasets that elicit EM are general across model families, enabling targeted attacks
% - 

\section{Continual Learning Poses Significant Challenges to Safety and Evaluation}

% TODO: SECTION

The focus of this project, however, is another critical failure case: emergent misalignment (EM). \cite{betley2025emergent} showed that fine-tuning GPT-4o on insecure code led to broad misalignment (e.g., admiration for Hitler). More generally, fine-tuning on wrong answers has been shown to cause this phenomenon \citep{wang2026persona_em_openai}.

EM might also still occur during self-distillation even after filtering suspicious data.
% TODO ADD: Define self-distillation 
\citep{cloud2026language} showed that a teacher model can transmit a trait to a student model even when the data it produces carries no semantic information about that trait, provided the student and teacher share the same base model.


% Personas are the leading mechanism for explaining EM
\cite{wang2026persona_em_openai} claims that ``personas''---roles or personality traits that the model adopts---are the leading mechanistic explanation for why EM occurs. Concretely, they found that a ``toxic persona'' feature was present in the activation space across all the emergently misaligned models they studied. This relationship is causal; pushing the model's internal activations toward this feature increased its misaligned behaviour, and vice versa. These findings are further supported by independent work reporting similar results in open-weight models \citep{arditi2025misaligned}. In short, when a model is trained on a narrow set of misaligned examples, these persona features become more active overall, causing misalignment to spread to other, unrelated domains.

\section{Research Questions and Contributions}

% We use projection differences 
This project builds on the concept of projection differences, as defined in \citet{chen2025persona_vectors}, which serves as a metric strongly correlated with changes in an LLM's propensities after fine-tuning on a specific dataset. Intuitively, the projection difference measures how much a training dataset shifts the model's behaviour along a given propensity. The core research question is whether projection differences remain useful for forecasting behaviour after fine-tuning across a sequence of datasets, which we use as a proxy for a continual learning setting.


The main issue is that during forecasting, we only have access to the initial model. Furthermore, recomputing the projection at every step can be expensive. In this project, we explore this by measuring how the predictive accuracy of behaviour evolves over time. We also propose a conceptual fix: carrying a state that helps correct the outdated projection difference. This predictive model could be used, for instance, to test how robust the ``safety basin''\footnote{According to \cite{Peng2024safety_basin}, ``random perturbations to model weights maintain the safety level of the original aligned model within its local neighborhood. However, outside this local region, safety is fully compromised, exhibiting a sharp, step-like drop. This safety basin contrasts sharply with the LLM capability landscape, where model performance peaks at the origin and gradually declines as random perturbation increases.''} \citep{Peng2024safety_basin} of the current model is by analysing multiple trajectories in a simulator.

\begin{itemize}
    \item \textbf{RQ1 (Decay)}: How does predictive accuracy of projection differences computed with $\mathcal{M}_0$ degrade with sequence length, and measured drift $(\rho, r)$?
    \item \textbf{RQ2 (History-Dependence)}: Does the persona induced by the last dataset always win, or do previous SFTs also influence the result? Some sub-questions:
    \begin{itemize}
        \item What are the effects of quantity (sequence length) and diversity of the datasets (e.g., training on hallucination + evil vs twice on evil)?
        \item Even if the model gets realigned, is it easier to misalign it in future iterations, and do the activations come back to their original position?
    \end{itemize}
\end{itemize}

For this reason, in this project, we analyse how long projection differences retain their predictive power across sequences of fine-tuning steps. This is the unexplored ``chaotic sensitivity'' problem mentioned in \cite{pacchiardi2026continual}.

The main theoretical limitation of this approach, however, is representation drift. Projection differences that rely on the base model can easily become outdated. In other words, there is an open question of whether fine-tuning on a dataset always modifies the model's behaviour in the same direction and to the same extent, or whether the order matters.

\section{Project's Structure}

\begin{itemize}
    \item \Cref{ch:background_related_work} discusses background concepts necessary to understand the rest of the project. In particular, we introduce what neural networks are and how they are typically trained. Then, we briefly explain how current transformer models work and how LLMs are trained. We then discuss a promising direction (self-distillation) that could allow users to have models that continually evolve to adapt to their particular use cases. Then, we explore the limitations of current evaluation and mechanistic interpretability techniques and discuss why they fail in this type of scenario.
    \item \Cref{ch:methodology} introduces the methodology
    \item \Cref{ch:results} presents the results of our experiments and discusses their implications.
\end{itemize}