\chapter{Experiments and Results} % 5
\label{ch:experiments}
% Walkthrough of the experiments 
In this chapter, we present the experiments we conducted to answer the research questions and their results. Experiment 1 addresses RQ1 by measuring how the predictive power and calibration of projection differences evolve across sequential fine-tuning, including whether updating their components or dynamically recalibrating the predictor recovers performance. Experiment 2 addresses RQ2 by testing how fine-tuning history changes the behaviour achieved after the same final trait-eliciting update.

Quantifying the decay of projection differences determines how much confidence we can place in predictions made by a predictor calibrated on the initial model, $f_0$, and when that predictor needs to be updated. Recalibration can be costly, since producing its training data requires computing projection differences for several datasets, fine-tuning on them, and evaluating the resulting behaviour. Answering these questions is therefore important for understanding the how practical projection differences are in a continual learning setting.


\section{Implementation Validation}
\label{sec:validation}
% TODO: Make the paragraph and caption consistent about whether the plotted outcome is behaviour or behaviour change.
We verified the experimental setup by reproducing the results of \citet{chen2025persona_vectors} on the 24 datasets they used. The results are shown in \Cref{fig:validation}. The correlation between projection difference and next-step behaviour is consistent with that reported in the original paper.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_validation.pdf}
    \caption{Projection difference $\Delta P_0$ against next-step behaviour $b_1$ after fine-tuning the initial model on each of the 24 datasets, for evil (left) and sycophancy (right). Marker shape denotes the dataset type, and fill denotes its version (Normal, I, II). Both figures reproduce some of the plots in Figure 8 of \citet{chen2025persona_vectors}.}
    \label{fig:validation}
\end{figure}

\section{Experiment 1: Measuring the Robustness of Projection Differences}
\label{sec:robustness}
% TODO: Specify which projection differences predict behaviour and which predict behaviour change, and how these predictions are compared on the same scale.
\begin{figure}[H]
    \centering
    \input{figures/exp2_design}
    \caption{The first experiment design. Three six-update trunks start from the same
    initial model $\mathcal{M}_0$. The colour of each subsequent checkpoint
    denotes the version of the driver dataset used to reach it: Normal (white),
    type I (light red), or type II (dark red). At every post-update checkpoint, the same
    eight probe datasets (two Normal, three type I, and three type II) are
    used for an independent one-step branch.}
    \label{fig:exp2-design}
\end{figure}

In this experiment, we run three trajectory trunks, which we name A, B, and C. Trunk A alternates between normal and Misalignment II datasets; trunk B trains on two Misalignment I datasets before realigning with a normal dataset. Trunk C trains only on normal datasets. Each is composed of six fine-tuning steps. \Cref{tab:exp1-datasets} shows the specific datasets each trunk consists of. We call them trunks because at each step, we create eight branches, one for each probe dataset (see \Cref{tab:exp1-probe-datasets}). Each branch is a fine-tuning event on the corresponding probe dataset. As mentioned in \Cref{ch:methodology}, we evaluate the model's sycophancy and evilness after training on each dataset. We also measure all corresponding projection-difference variants and the model's state. These eight probe datasets allow us to compute and plot the correlation of each projection-difference variant and the next-step behaviour (i.e., to approximate \Cref{fig:validation} but at each checkpoint). When evaluating the accuracy of the predictive model, however, we cannot use the correlation but an error. We used the root mean square error (RMSE) instead. Intuitively, the correlation measures how well a perfectly calibrated regressor would perform, whereas the RMSE is how it actually performed. If the correlation is not high, no linear predictor can achieve good performance.

% TABLE: DATASETS ORDERING
\begin{table}[H]
    \centering
    \scriptsize
    \setlength{\tabcolsep}{4pt}
    \begin{tabular}{clll}
        Step & Trunk A & Trunk B & Trunk C \\
        \hline
        1 & Hallucination (II) & Evil (I) & Evil (Normal) \\
        2 & Evil (Normal) & Insecure code (I) & Hallucination (Normal) \\
        3 & Opinions (II) & Hallucination (Normal) & GSM8K (Normal) \\
        4 & GSM8K (Normal) & GSM8K (I) & Math (Normal) \\
        5 & Sycophancy (II) & Math (I) & Opinions (Normal) \\
        6 & Math (Normal) & Opinions (Normal) & Sycophancy (Normal) \\
    \end{tabular}
    \caption{The ordered training datasets for each trajectory.}
    \label{tab:exp1-datasets}
\end{table}

% TABLE: PROBE DATASETS
\begin{table}[H]
    \centering
    \scriptsize
    \begin{tabular}{lll}
        Normal & Misalignment I & Misalignment II \\
        \hline
        Insecure code (Normal) & Hallucination (I) & Evil (II) \\
        Medical (Normal) & Opinions (I) & GSM8K (II) \\
        & Sycophancy (I) & Math (II) \\
    \end{tabular}
    \caption{The eight fixed probe datasets used for one-step branches at every checkpoint, grouped by dataset type.}
    \label{tab:exp1-probe-datasets}
\end{table}

\subsection{Correlation of Projection Differences and Behaviour}

Overall, we found that all projection variants presented in \Cref{sec:deltap-variants}, including $Delta P_0$, remained highly correlated with behaviour for almost all trajectories and steps. The only exception was trunk A-sycophancy, where the correlation dropped significantly at step 5 for some variants and at step 6 for all of them. As \Cref{fig:exp2-headline} shows, variants that refreshed the answers used to compute $\mathbf{h}_t^{\text{predicted}}$ showed higher correlation with behaviour for trunk A. Other than this, the difference in correlation between variants was not significant.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_headline.pdf}
    \caption{Correlation at each checkpoint for the six refreshed projection-difference variants defined in \Cref{sec:deltap-variants}. Rows show driver trunks, columns show persona-vector variants, and three-row blocks show the measured traits. Line style identifies the predicted-answer source, and the label at the end of each curve gives its mean correlation over its respective trunk's checkpoints.}
    \label{fig:exp2-headline}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.6\linewidth]{plots/real/exp2/exp2_decay_summary.pdf}
    \caption{Correlation between $\Delta P_0$ and the six refreshed projection-difference variants defined in \Cref{sec:deltap-variants}, combining both traits and all three trunks. Each bar shows the mean correlation across all checkpoints. The horizontal whisker shows one standard deviation on either side of the mean (truncated at $r=1$).}
    \label{fig:exp2-decay-summary}
\end{figure}

Interestingly, $\Delta P_0$, which does not make use of the updated model $\mathcal{M}_t$, maintained similar performance to the variants that did not refresh $\mathbf{h}_t^{\text{predicted}}$, as \Cref{fig:exp2-decay-summary} and \Cref{fig:exp2_decay_grid} show. \Cref{fig:exp2_decay_grid} compares $\Delta P_0$ with $\Delta P_t^{t\leftarrow0,[t]}$, which was the variant that achieved the highest mean correlation across all traits and trunks. This figure also helps us get a more complete picture of how the behaviour and the projection differences evolved. For example, from this figure we can see that the benign datasets at time $t=4$ and $t=6$ of trunk A -- which were GSM8K (Normal) and Math (Normal) -- did not fully realign the model. We also see how refreshing the answers of $\mathbf{h}_t^{\text{predicted}}$ helped at $t=5$ in trunk A-sycophancy. 

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_decay_grid.pdf}
    \caption{Behaviour after probe fine-tuning, $b_{t+1}$, against $\Delta P_0$ (blue) and $\Delta P_t^{t\leftarrow0,[t]}$ (orange) at each checkpoint; the variants are defined in \Cref{sec:deltap-variants}. Columns show checkpoints and rows show driver trunks, in separate blocks for evil and sycophancy. Fitted lines are labelled with Pearson's $r$, and the horizontal black line marks $b_t$.}
    \label{fig:exp2_decay_grid}
\end{figure}

\subsection{Evaluating a Behaviour Forecaster}
\label{subsec:forecaster_eval}
In practice, we cannot train on 8 probe datasets at every step to fit the linear model that maps projection differences to behaviour, as this is computationally more expensive than simply fine-tuning the model on the next dataset. Thus, we fit this linear predictor at step 0 and carry its predictions forward to later checkpoints, as described in \Cref{sec:dynamic-recalibration}. We then use the checkpoint state to attempt to improve its calibration by fitting a recalibration function $c_t(\mathbf{z}_t)$ whose output scales $\beta_0$.

We evaluate the forecaster using leave-one-out cross-validation. For a held-out trajectory and probe dataset $j$, the forecaster is fitted only on checkpoints from the other trajectories. Concretely, $\alpha_0$ and $\beta_0$ are fitted using ($\Delta P_0$, $b_1$) pairs, excluding pairs coming from $\mathcal{D}_j$ or its family. For example, if $\mathcal{D}_j$ is of type Misalignment I, we also exclude its type II and normal versions as they share the same questions.

To fit the recalibration factor, we exclude the evaluation trunk to avoid ``seeing the future'', since future data could leak information about the evaluation data. Similarly, we exclude data points coming from $\mathcal{D}_j$ even if they are from a different trunk. Every training target $c^{*}_{t,-j}$ is computed from the other seven probe datasets (note that none of the 8 probe datasets shares a family, so they are independent of each other). This way, we maximise the number of data points we can use for training while preventing data leakage. \Cref{fig:exp2-loo} illustrates this evaluation design. 

\begin{figure}[htbp]
    \centering
    \input{figures/exp2_loo}
    \caption{One fold of the leave-one-out evaluation of the recalibration
    factor, holding out one trunk and one probe dataset $\mathcal{D}_j$. As in
    \Cref{fig:exp2-design}, the fan above a checkpoint is its eight one-step
    probe branches. On the two fitted trunks (grey), a filled dot is a probe
    that enters the fit, and the cross is $\mathcal{D}_j$, which is excluded. On the held-out trunk (red), white dots are probes that were measured but go unused in
    this fold, and the filled dot is $\mathcal{D}_j$, the one prediction
    scored.}
    \label{fig:exp2-loo}
\end{figure}

The fitted regressor is then applied to the held-out trajectory and used solely to predict probe $j$. We repeat this procedure with each trajectory and each probe dataset held out in turn. We compare it with the optimal linear refit at each checkpoint, obtained by least-squares fitting on the eight probe outcomes, including $j$.

\Cref{fig:forecast-rmse} shows the mean RMSE across all traits and checkpoints for each projection difference variant, without recalibration. From this figure, we can identify that projection differences that regenerate answers before computing $\mathbf{h}_t^{\text{predicted}}$ are better predictors of behaviour change ($\Delta b_{t+1}$). On the other hand, those who encoded answers generated by $\mathcal{M}_0$ predicted $b_{t+1}$ better, even though they were encoded by the updated model $\mathcal{M}_t$. To ensure fair comparisons and simplify the visualisations, future experiments fit each projection difference to $\Delta b_{t+1}$ if they use $\mathbf{h}^{\text{predicted}}_{t\leftarrow t}$ and to $b_{t+1}$ otherwise. 

\begin{figure}[htbp]
    \centering
    \includegraphics[width=0.75\textwidth]{plots/real/exp2/exp2_forecast_rmse_bar.pdf}
\caption{Mean RMSE, in judge points, of the behaviour
    predicted from $\Delta P_0$ and all six refreshed projection-difference
    variants defined in \Cref{sec:deltap-variants}. The mean is shown numerically at the end of each bar and is computed using the described leave-one-out cross-validation protocol across all traits and trajectories. Error bars show one standard deviation across all RMSE values.}
    \label{fig:forecast-rmse}
\end{figure}

\begin{figure}[htbp]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_headline_rmse.pdf}
    \caption{RMSE, in judge points, at each checkpoint for the six refreshed
    projection-difference variants defined in \Cref{sec:deltap-variants}, using
    the leave-one-out predictions of the map $f_0$ fitted at $\mathcal{M}_0$. The label at the end of each curve
    gives its mean RMSE over checkpoints.}
    \label{fig:exp2-headline-rmse}
\end{figure}

For the state-evolution analysis, we examine the winner projection-difference variant $\Delta P_t^{t\leftarrow0,[t]}$, together with the checkpoint-state variants $\mathbf{z}_t^{[0,0]}$ and $\mathbf{z}_t^{[t,0]}$. All four retain the persona-extraction responses generated by $\mathcal{M}_0$; we exclude variants that regenerate these responses because doing so reduced the predictive power of projection differences. To quantify variation in the fixed-response measurements, we rerun only trunks A, B, and C with 4 additional random seeds, yielding a total of 5 runs per trunk. Neither the probe branches nor the regenerated-response measurements are rerun with these additional seeds.



% The same encoding laid out the other way -- all six variants in one panel
% per (trait, trunk), three rows shorter -- is emitted alongside this one as
% plots/real/exp2/exp2_headline_overlaid.pdf. Swap the file name above to use it.

\subsection{Dynamic Recalibration}
% TODO: Explain the main RMSE and bias results shown here and in the appendix, including whether strong correlation coexists with inaccurate predictions.
% TODO: Discuss the appendix's state-based correction results: which corrections improve on the initial predictor, by how much, and where they fail.
% TODO: Summarise which state measurements are associated with correlation changes in the mechanism figure.


% The same numbers as one flat ranking, every bar labelled with its fitted
% map, its projection and its target, are emitted alongside this one as
% plots/real/exp2/exp2_forecast_rmse_bar_ranked.pdf. That layout is the only
% one that orders $f_0$ and $f_t$ against each other; it is twice as tall.
% Swap the file name above to use it.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_recalibration_grid.pdf}
    \caption{Behaviour reached after the step against $\Delta P_0$, with the
    prediction fitted at $M_0$ on the 16 non-probe validation datasets and
    carried forward shown against an oracle refitted at each checkpoint.}
    \label{fig:recalibration-grid}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_forecast_grid.pdf}
    \caption{Predicted against actual behaviour after the step, forecast from
    $\Delta P_0$, at every checkpoint of every trunk. A forecast is right when
    its cloud lies on the diagonal, so vertical spread about that line is the
    RMSE printed beside it and a cloud sitting wholly above or below it is a
    systematic bias. Blue gives the leave-one-family-out $M_0$ predictions;
    orange is the same probes refitted at the checkpoint. The diagonal is the
    only line drawn: a forecaster's own line is already spent placing the
    points along the horizontal axis, which is what makes the diagonal the
    thing to read against.}
    \label{fig:forecast-grid}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2\_mechanism.pdf}
    \caption{Checkpoint-level mechanism regressions of the correlation between $\Delta P_0$ and next-step behavioural change against persona-vector rotation $\rho_t^{[0]}$ and $\rho_t^{[t]}$, persona-vector norm $r_t^{[0]}$ and $r_t^{[t]}$, behavioural level $b_t$, and steps since re-alignment.}
\end{figure}

% \section{Phase Contrast}

% \begin{figure}[H]
%     \centering
%     \includegraphics[width=\linewidth]{plots/real/exp2/exp2_phase_contrast.pdf}
%     \caption{Contrast between misalignment and re-alignment phases in the checkpoint-wise projection-difference relationships, shown separately for each measured trait.}
% \end{figure}

\subsection{State Evolution}
% TODO: Describe how projection differences and state components change after trait-eliciting and normal updates, and whether they return towards their initial values.
% TODO: Report the comparison between the two state variants specified in the design; the current state figure describes only the fixed-response variant.
% TODO: Describe consistency across seeds and define the plotted bands.

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_drift_delta_hat_p.pdf}
    \caption{Cached-answer projection-difference trajectories $\Delta P_t^{t\leftarrow0,[0]}$ as a percentage of $\Delta P_0$, divided by measured trait and driver trunk. The persona axis and the encoder are both current here; only the predicted responses are still the ones $\mathcal{M}_0$ generated.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_drift_delta_p.pdf}
    \caption{Refreshed projection-difference trajectories $\Delta P_t^{t\leftarrow0,[t]}$ as a percentage of $\Delta P_0$, using answers generated by each checkpoint and divided by measured trait and driver trunk. Everything but the persona-extraction text is taken at $\mathcal{M}_t$.}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=\linewidth]{plots/real/exp2/exp2_drift_z.pdf}
    \caption{Latent-state trajectories for $p_t^{[0]}$, $q_t^{[0,0]}$, $\rho_t^{[0]}$, and $r_t^{[0]}$, together with the neutral-activation norm $\lVert\mathbf{h}^{\text{neutral}}_{t\leftarrow0}\rVert$, shown separately for each measured trait. Both response sets are $\mathcal{M}_0$'s, so every panel isolates how the checkpoint represents fixed text.}
\end{figure}

\section{Experiment 2: History Dependence}
\label{sec:hysteresis}
\subsection{Design}
% TODO: Specify the target datasets, normal datasets, selection of the different earlier dataset, seed counts, and completed runs.
% TODO: Specify how runs are paired across arms by final dataset and seed.
% TODO: State how final behaviour and final-step behaviour change are compared, how results are aggregated, and how uncertainty is measured.
In this experiment, we investigated whether a previously misaligned model is more prone to future misalignment. Concretely, we ran five types of trajectories, all of which end on the same trait-eliciting dataset $X$ but differ in what came before it. The five types are the following:
\begin{itemize}
    \item $X$: the initial model is fine-tuned on $X$.
    \item $N\ X$: the model is first fine-tuned on a normal dataset, and then on $X$.
    \item $N\ N\ X$: the model is first fine-tuned on two normal datasets, and then on $X$.
    \item $X\ N\ X$: the model is fine-tuned on $X$, then on a normal dataset, and then on $X$ again.
    \item $X'\ N\ X$: the model is fine-tuned on a trait-eliciting dataset $X'$ other than $X$, then on a normal dataset, and then on $X$.
\end{itemize}


\begin{figure}[H]
    \centering
    \input{figures/exp3_design}
    \caption{The second experiment design. Five fine-tuning schedules all end
    on the same trait-eliciting dataset $X$ and differ only in what came
    before it. Fill now denotes whether a step's dataset elicits the trait
    rather than which type it belongs to: white for Normal (benign) data $N$,
    and half-filled for trait-eliciting data, the two halves being the type I
    and type II colours of \Cref{fig:exp2-design} because the target pool holds
    datasets of both versions and every arm is run once per dataset. Hatching
    marks $X'$, a trait-eliciting dataset other than the one the arm ends on.
    Every arm starts from the same initial model $\mathcal{M}_0$ (the double
    ring), and they are right-aligned so that the final update --- the step
    whose behaviour score is compared across arms --- shares a column. \emph{Baseline} meets $X$
    for the first time. \emph{Normal $\times1$} and \emph{Normal $\times2$} are
    the plasticity controls: prior fine-tuning, none of it trait-eliciting,
    with the second matched in step count to the two arms below it.
    \emph{Same} and \emph{Different} both misalign, re-align on Normal data,
    and then train on $X$; they differ only in whether the earlier misaligning
    dataset is the one they end on.}
    \label{fig:exp3-design}
\end{figure}

\subsection{History-Dependence Results}
% TODO: Show behaviour before and after the final update, checking whether the preceding normal update actually restored low trait scores.
% TODO: Compare one and two normal updates with the direct-update baseline, then compare prior trait exposure with the control matched on update count.
% TODO: Compare repeated exposure to the same dataset with exposure to a different one; report effect sizes and variation across targets, traits, and seeds.
% TODO: Give a concise answer to each RQ2 sub-question, separating observed final scores from evidence of greater susceptibility to the final update.
